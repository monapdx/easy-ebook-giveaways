import Card from '../../../components/ui/Card';

export default function GiveawaySuccessPage() {
  return (
    <div className="public-page">
      <Card>
        <div className="stack">
          <h1>Success</h1>
          <p>
            Your entry was received. If you were not redirected automatically, use the download link
            from the giveaway page or check your inbox for the delivery email (when email is
            enabled).
          </p>
        </div>
      </Card>
    </div>
  );
}
